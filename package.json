{
  "name": "sellhays",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "mv .env staging.env && mv production.env .env && vue-cli-service build && firebase deploy && firebase use default && mv .env production.env && mv staging.env .env"
  },
  "dependencies": {
    "firebase": "^5.8.5",
    "moment": "^2.24.0",
    "tailwindcss": "^0.7.4",
    "vue": "^2.6.6",
    "vue-router": "^3.0.1",
    "vue-trix": "^0.7.4",
    "vue2-google-maps": "^0.10.6",
    "vuejs-datepicker": "^1.5.4",
    "vuex": "^3.0.1"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "^3.4.0",
    "@vue/cli-service": "^3.4.0",
    "fibers": "^3.1.1",
    "sass": "^1.16.0",
    "sass-loader": "^7.1.0",
    "vue-template-compiler": "^2.5.21"
  }
}
